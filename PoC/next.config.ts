import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Oculta la burbuja de desarrollo de Next durante la demo (los errores siguen mostrándose).
  devIndicators: false,
};

export default nextConfig;
